# Likely duplicates

Generated by `merge.py`. Nothing here was merged. For each pair, either add a
`merge` override to `data/overrides.json`, or a `distinct` override to hide it.

## Same term in another form or language (25)

- `art` — **art** (word, English; 1 episode(s))
  `kunst` — **Kunst** (word, German, original: Kunst; 1 episode(s))
  - merge: `{"op": "merge", "from": "kunst", "into": "art"}`
  - keep apart: `{"op": "distinct", "slugs": ["art", "kunst"]}`

- `avon` — **Avon** (name, Celtic; 1 episode(s))
  `fleuve` — **fleuve** (word, French; 1 episode(s))
  - merge: `{"op": "merge", "from": "fleuve", "into": "avon"}`
  - keep apart: `{"op": "distinct", "slugs": ["avon", "fleuve"]}`

- `avon` — **Avon** (name, Celtic; 1 episode(s))
  `ouse` — **Ouse** (name, Old Norse; 1 episode(s))
  - merge: `{"op": "merge", "from": "ouse", "into": "avon"}`
  - keep apart: `{"op": "distinct", "slugs": ["avon", "ouse"]}`

- `avon` — **Avon** (name, Celtic; 1 episode(s))
  `river` — **river** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "river", "into": "avon"}`
  - keep apart: `{"op": "distinct", "slugs": ["avon", "river"]}`

- `avon` — **Avon** (name, Celtic; 1 episode(s))
  `riviere` — **rivière** (word, French; 1 episode(s))
  - merge: `{"op": "merge", "from": "riviere", "into": "avon"}`
  - keep apart: `{"op": "distinct", "slugs": ["avon", "riviere"]}`

- `dead-trousers` — **dead trousers** (idiom, German, original: tote Hose; 1 episode(s))
  `die-toten-hosen` — **Die Toten Hosen** (name, German, original: Die Toten Hosen; 1 episode(s))
  - merge: `{"op": "merge", "from": "die-toten-hosen", "into": "dead-trousers"}`
  - keep apart: `{"op": "distinct", "slugs": ["dead-trousers", "die-toten-hosen"]}`

- `desert` — **desert** (word, English; 2 episode(s))
  `sahara` — **Sahara** (name, Arabic; 1 episode(s))
  - merge: `{"op": "merge", "from": "sahara", "into": "desert"}`
  - keep apart: `{"op": "distinct", "slugs": ["desert", "sahara"]}`

- `east` — **east** (word, English; 1 episode(s))
  `est` — **est** (word, French; 1 episode(s))
  - merge: `{"op": "merge", "from": "east", "into": "est"}`
  - keep apart: `{"op": "distinct", "slugs": ["east", "est"]}`

- `fleuve` — **fleuve** (word, French; 1 episode(s))
  `ouse` — **Ouse** (name, Old Norse; 1 episode(s))
  - merge: `{"op": "merge", "from": "fleuve", "into": "ouse"}`
  - keep apart: `{"op": "distinct", "slugs": ["fleuve", "ouse"]}`

- `fleuve` — **fleuve** (word, French; 1 episode(s))
  `river` — **river** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "fleuve", "into": "river"}`
  - keep apart: `{"op": "distinct", "slugs": ["fleuve", "river"]}`

- `fleuve` — **fleuve** (word, French; 1 episode(s))
  `riviere` — **rivière** (word, French; 1 episode(s))
  - merge: `{"op": "merge", "from": "riviere", "into": "fleuve"}`
  - keep apart: `{"op": "distinct", "slugs": ["fleuve", "riviere"]}`

- `ghost` — **ghost** (word, English; 1 episode(s))
  `lemur` — **lemur** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "lemur", "into": "ghost"}`
  - keep apart: `{"op": "distinct", "slugs": ["ghost", "lemur"]}`

- `har` — **hár** (word, Old Norse, original: hár; 1 episode(s))
  `shark` — **shark** (word, English; 2 episode(s))
  - merge: `{"op": "merge", "from": "har", "into": "shark"}`
  - keep apart: `{"op": "distinct", "slugs": ["har", "shark"]}`

- `har` — **hár** (word, Old Norse, original: hár; 1 episode(s))
  `tiburon` — **tiburón** (word, Spanish, original: tiburón; 1 episode(s))
  - merge: `{"op": "merge", "from": "tiburon", "into": "har"}`
  - keep apart: `{"op": "distinct", "slugs": ["har", "tiburon"]}`

- `muscle` — **muscle** (word, English; 3 episode(s))
  `mussel` — **mussel** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "mussel", "into": "muscle"}`
  - keep apart: `{"op": "distinct", "slugs": ["muscle", "mussel"]}`

- `nord` — **nord** (word, French; 1 episode(s))
  `north` — **north** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "north", "into": "nord"}`
  - keep apart: `{"op": "distinct", "slugs": ["nord", "north"]}`

- `ouest` — **ouest** (word, French; 1 episode(s))
  `west` — **west** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "ouest", "into": "west"}`
  - keep apart: `{"op": "distinct", "slugs": ["ouest", "west"]}`

- `oursin` — **oursin** (word, French, original: oursin; 1 episode(s))
  `sea-urchin` — **sea urchin** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "sea-urchin", "into": "oursin"}`
  - keep apart: `{"op": "distinct", "slugs": ["oursin", "sea-urchin"]}`

- `ouse` — **Ouse** (name, Old Norse; 1 episode(s))
  `river` — **river** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "river", "into": "ouse"}`
  - keep apart: `{"op": "distinct", "slugs": ["ouse", "river"]}`

- `ouse` — **Ouse** (name, Old Norse; 1 episode(s))
  `riviere` — **rivière** (word, French; 1 episode(s))
  - merge: `{"op": "merge", "from": "riviere", "into": "ouse"}`
  - keep apart: `{"op": "distinct", "slugs": ["ouse", "riviere"]}`

- `river` — **river** (word, English; 1 episode(s))
  `riviere` — **rivière** (word, French; 1 episode(s))
  - merge: `{"op": "merge", "from": "riviere", "into": "river"}`
  - keep apart: `{"op": "distinct", "slugs": ["river", "riviere"]}`

- `savanna` — **savanna** (word, English; 1 episode(s))
  `steppe` — **steppe** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "savanna", "into": "steppe"}`
  - keep apart: `{"op": "distinct", "slugs": ["savanna", "steppe"]}`

- `shark` — **shark** (word, English; 2 episode(s))
  `tiburon` — **tiburón** (word, Spanish, original: tiburón; 1 episode(s))
  - merge: `{"op": "merge", "from": "tiburon", "into": "shark"}`
  - keep apart: `{"op": "distinct", "slugs": ["shark", "tiburon"]}`

- `shiba` — **shiba** (word, Japanese; 1 episode(s))
  `shiba-inu` — **Shiba Inu** (word, Japanese; 1 episode(s))
  - merge: `{"op": "merge", "from": "shiba-inu", "into": "shiba"}`
  - keep apart: `{"op": "distinct", "slugs": ["shiba", "shiba-inu"]}`

- `south` — **south** (word, English; 1 episode(s))
  `sud` — **sud** (word, French; 1 episode(s))
  - merge: `{"op": "merge", "from": "south", "into": "sud"}`
  - keep apart: `{"op": "distinct", "slugs": ["south", "sud"]}`

## Spelling variant (13)

- `balter` — **balter** (word, English; 1 episode(s))
  `baxter` — **Baxter** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "baxter", "into": "balter"}`
  - keep apart: `{"op": "distinct", "slugs": ["balter", "baxter"]}`

- `blutwurst` — **Blutwurst** (word, German, original: Blutwurst; 1 episode(s))
  `bratwurst` — **bratwurst** (word, German; 1 episode(s))
  - merge: `{"op": "merge", "from": "bratwurst", "into": "blutwurst"}`
  - keep apart: `{"op": "distinct", "slugs": ["blutwurst", "bratwurst"]}`

- `canvas` — **canvas** (word, English; 2 episode(s))
  `canvass` — **canvass** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "canvass", "into": "canvas"}`
  - keep apart: `{"op": "distinct", "slugs": ["canvas", "canvass"]}`

- `coney` — **coney** (word, English; 1 episode(s))
  `convey` — **convey** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "convey", "into": "coney"}`
  - keep apart: `{"op": "distinct", "slugs": ["coney", "convey"]}`

- `desert` — **desert** (word, English; 2 episode(s))
  `dessert` — **dessert** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "dessert", "into": "desert"}`
  - keep apart: `{"op": "distinct", "slugs": ["desert", "dessert"]}`

- `gentile` — **gentile** (word, English; 1 episode(s))
  `gentle` — **gentle** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "gentile", "into": "gentle"}`
  - keep apart: `{"op": "distinct", "slugs": ["gentile", "gentle"]}`

- `horologe` — **horologe** (word, English; 1 episode(s))
  `horologer` — **horologer** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "horologer", "into": "horologe"}`
  - keep apart: `{"op": "distinct", "slugs": ["horologe", "horologer"]}`

- `poppet` — **poppet** (word, English; 2 episode(s))
  `puppet` — **puppet** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "puppet", "into": "poppet"}`
  - keep apart: `{"op": "distinct", "slugs": ["poppet", "puppet"]}`

- `river` — **river** (word, English; 1 episode(s))
  `rover` — **Rover** (name, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "rover", "into": "river"}`
  - keep apart: `{"op": "distinct", "slugs": ["river", "rover"]}`

- `shirt` — **shirt** (word, English; 1 episode(s))
  `skirt` — **skirt** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "skirt", "into": "shirt"}`
  - keep apart: `{"op": "distinct", "slugs": ["shirt", "skirt"]}`

- `soliloquy` — **soliloquy** (word, English; 1 episode(s))
  `somniloquy` — **somniloquy** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "somniloquy", "into": "soliloquy"}`
  - keep apart: `{"op": "distinct", "slugs": ["soliloquy", "somniloquy"]}`

- `thrimilce` — **Þrimilce** (name, English, original: Þrimilce; 1 episode(s))
  `thrimilci` — **Thrimilci** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "thrimilci", "into": "thrimilce"}`
  - keep apart: `{"op": "distinct", "slugs": ["thrimilce", "thrimilci"]}`

- `trivia` — **trivia** (word, English; 1 episode(s))
  `trivial` — **trivial** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "trivial", "into": "trivia"}`
  - keep apart: `{"op": "distinct", "slugs": ["trivia", "trivial"]}`

## Mentions that disagree on language or type

These mentions were grouped under one slug but were classified differently.
The majority value is used; fix it with a `set` override, or split the entry
with a `rename` override limited to one `episode_id`.

- `blotmonath` type: word ×1, name ×1
- `chiaroscuro` language: English ×1, Italian ×1
- `corgi` language: Welsh ×1, English ×1
- `dachshund` language: German ×1, English ×1
- `dandelion` type: word ×1, name ×1
- `nachos` language: English ×1, Spanish ×1
- `saluki` language: Sumerian ×1, English ×1
