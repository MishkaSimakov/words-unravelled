# Likely duplicates

Generated by `merge.py`. Nothing here was merged. For each pair, either add a
`merge` override to `data/overrides.json`, or a `distinct` override to hide it.

## Same term in another form or language (1)

- `dead-trousers` — **dead trousers** (idiom, German, original: tote Hose; 1 episode(s))
  `die-toten-hosen` — **Die Toten Hosen** (name, German, original: Die Toten Hosen; 1 episode(s))
  - merge: `{"op": "merge", "from": "die-toten-hosen", "into": "dead-trousers"}`
  - keep apart: `{"op": "distinct", "slugs": ["dead-trousers", "die-toten-hosen"]}`

## Spelling variant (2)

- `soliloquy` — **soliloquy** (word, English; 1 episode(s))
  `somniloquy` — **somniloquy** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "somniloquy", "into": "soliloquy"}`
  - keep apart: `{"op": "distinct", "slugs": ["soliloquy", "somniloquy"]}`

- `trivia` — **trivia** (word, English; 1 episode(s))
  `trivial` — **trivial** (word, English; 1 episode(s))
  - merge: `{"op": "merge", "from": "trivial", "into": "trivia"}`
  - keep apart: `{"op": "distinct", "slugs": ["trivia", "trivial"]}`
